# ChangeLog — правила 8885 → 8885A

Набор правил `rules/8885A.rule` — сбалансированный вариант игры 8885.
Ниже описаны все изменения относительно `rules/8885.rule` и их обоснование.

> **Обновление (конечная жизнь, UPDATE0.md):** с введением конечной жизни
> (секции `Смертность:`, `Долголетие:`, `Успех убийства:`, `Потомство:` и
> параметры `Награда за убийство`/`Штраф за размножение` — см. `LANGUAGE.md`
> и `SIMS.md`) статистика ниже переснята для новых параметров: `Смертность
> 3–5%`, `Долголетие 60–80`, `Потомство 0:40/1:60`, `Штраф 18%`, `Награда
> 10%`, `Успех убийства 50–70%`. Обоснование выбора закона старения и
> подбора параметров — в `SIMS.md`.

## Проблема оригинала

При многократном прогоне `8885.rule` (500 партий):

- **Белые доминируют**: «Победа вида: Белый» — в 34% партий; в среднем белые
  занимают **58% поля** (жёлтые — 15%, красные — 6%, остальные — единицы %).
- **Жёлтые стабильно вторые** (15% поля).
- **Партии заканчиваются очень быстро** — в среднем за 8 ходов (поле
  заполняется экспоненциально).

Причина: у белых чистое размножение «со своими» (потомок всегда белый),
которое не разбавляется гибридами и не сдерживается другими видами:
красные и гибриды не убивают, жёлтые не убивают белых, синие убивают только
доминирующего (и их мало), чёрные слабы. В итоге белые «снежным комом»
обгоняют всех ещё на первых ходах.

## Цель и результат 8885A

Сделать партии разнообразнее: у разных видов должен быть шанс победить,
ни один вид не должен занимать более ~15–20% поля, а партии — длиться
дольше. Результат (100 партий с конечной жизнью, `tools/balance.hs`):

| Показатель | 8885.rule (конечная жизнь, лёгкая) | 8885A.rule (конечная жизнь, настроена) |
|---|---|---|
| Средняя доля белых в финале | 56% | 3% |
| Доля жёлтых | 14% | 30% (максимум) |
| Доля красных | 7% | 15% |
| Доля синих | 3% | 8% |
| Победы видов | Белый 28%, поле — 72% | Красный 22%, Жёлтый 19%, Бордовый 12%, Синий 9%, поле — 7%, вымирание — 22%, остальные — единицы % (Розовый, Голубой, Фиолетовый, Серый, Оранжевый, Чёрный, Белый) |
| Средняя длина партии | 8 ходов | 46 ходов |

Ни один вид не доминирует: победители варьируются (11 разных видов),
максимум по доле поля — 30% (жёлтые). Глубокие спады популяции
(репродуктивное истощение, см. `SIMS.md`) дают длинные партии, ценой
~22% партий, заканчивающихся вымиранием.

## Изменения и обоснование

### 1. Белые больше не размножаются только «со своими»

```
Партнёры: Белый: [Белый] -> [~чистый~] [Белый < 5];   →   Белый: [Белый, ~чистый~] -> [];
```

Чистое размножение белых (потомок всегда белый) было главным источником их
преимущества: у всех остальных видов смешанные пары дают гибридов и
разбавляют их численность, а белые росли «в чистоте». Теперь белые
размножаются и с другими чистыми видами (потомок — гибрид или цвет одного
из родителей), как и остальные.

### 2. У белых ослаблено убийство «смешанных»

```
Действия: Белый: ~смешанные~;   →   Белый: 20% ~смешанные~;
```

(Новый синтаксис DSL: `N% ~смешанные~` — убивать «смешанных» с
вероятностью N%, в остальном размножаться.) Раньше белые убивали любого,
кто только что размножался с чужим цветом, — это выкашивало гибриды и
мешало росту всех остальных. Теперь белые лишь изредка вмешиваются.

### 3. Жёлтые могут убивать белых

```
Убийство: Жёлтый: * кроме Жёлтый, Белый;   →   Жёлтый: *;
```

В оригинале у белых не было естественных врагов (красные и гибриды не
убивают, жёлтые исключали белых из целей, синие — только когда белые
доминируют, чёрные слабы). Разрешив жёлтым (главному хищнику) убивать
белых, мы даём белым противовес.

### 4. Чёрные и жёлтые могут убивать и своих (зависимость от плотности)

```
Убийство: Чёрный: * кроме Чёрный, Тёмно-синий, ...;   →   кроме Тёмно-синий, ...;
Убийство: Жёлтый: * кроме Жёлтый;                      →   Жёлтый: *;
```

Раньше «свои» были исключены из целей, поэтому самый многочисленный вид
никогда не страдал от собственных убийств — лидер рос без ограничений.
Теперь убийства взвешены по численности, и когда вид становится слишком
многочисленным, он начинает чаще убивать своих же — рост лидера
ограничивается естественным образом (как зависимость от плотности в
реальных экосистемах). Чёрные по-прежнему не убивают своих гибридов.

### 5. Повышена агрессивность синих (убийца доминирующего)

```
Действия: Синий: 15%;   →   Синий: 50%;
```

Синие убивают только доминирующий вид — это главный «стабилизатор»: лидер
постоянно подрезается. Раньше синих было слишком мало и их сдерживание не
работало. Теперь синие активнее, и их роль «убийцы лидера» реально влияет
на баланс (синие чаще других побеждают — 27%, но это максимум, не
доминирование).

### 6. Синие размножаются и с красными

```
Партнёры: Синий: [Синий, Белый, Жёлтый] -> [*];   →   [Синий, Белый, Жёлтый, Красный] -> [*];
```

Немного разбавляет синих (меньше чисто-синего потомства) и не даёт им
снежного кома.

### 7. Снижено согласие принимающей стороны у синих, чёрных и жёлтых

```
Симпатии:
  Белый < Синий: 100% → 80%;   Жёлтый < Синий: 100% → 80%;   Красный < Синий: 50% → 40%;
  ~гибрид~ < Чёрный: 100% → 80%;   * < Чёрный: 50% → 40%;
  * < Жёлтый: 100% → 60%;
```

Меньше согласия — больше отказов: при отказе с вероятностью 40%
(«Изнасилование») потомок всё же рождается, а с вероятностью 60% инициатор
погибает. Это замедляет рост популяции (партии длиннее) и добавляет
динамики. Красные по-прежнему соглашаются всегда; белые — с чистыми всегда
(но с гибридами — никогда, как в оригинале).

### 8. Вероятность гибрида снижена с 50% до 30%

```
Рождение: Красный < Синий -> Фиолетовый: 50% Красный: 25% Синий: 25%;
      →   Фиолетовый: 30% Красный: 35% Синий: 35%;
```

В оригинале половина смешанных пар давала гибрида, из-за чего чистые виды
сильно «разбавлялись» и не могли конкурировать с белыми. С 30% чистые виды
сохраняют больше потомства своего цвета (по 35%), что выравнивает их рост.
Гибриды остаются заметной частью поля (~40% суммарно), но не вытесняют
исходные виды.

### 9. Жёлтый с красным даёт оранжевого с 60% (сдерживание жёлтых)

```
Рождение: Красный < Жёлтый -> Оранжевый: 50% Красный: 25% Жёлтый: 25%;
      →   Оранжевый: 60% Красный: 20% Жёлтый: 20%;
```

Жёлтые размножаются с красными с той же вероятностью, что и со своими
(правило оригинала), а красные всегда согласны и никогда не убивают, поэтому
у жёлтых всегда есть надёжный партнёр — это давало им преимущество. Теперь
такие пары чаще дают оранжевого (гибрид), разбавляя жёлтых.

### 10. Порог победы снижен: `Победа: 555` → `150`

В оригинале порог 555 достигался только видом-доминантом (белыми) незадолго
до заполнения поля — отсюда «белые выигрывают, и игра быстро заканчивается».
С порогом 150 партия чаще завершается победой вида, причём победитель
варьируется (синие, красные, жёлтые, гибриды), а не только заполнением поля.

### 11. Вероятность изнасилования: `Изнасилование: 50%` → `40%`

Вместе с п. 7 замедляет рост: отказы чаще заканчиваются гибелью инициатора.

## Что не менялось

- Исходные виды, гибриды и их цвета — как в оригинале.
- Красные по-прежнему никогда не убивают и всегда соглашаются; гибриды не
  убивают по своей инициативе; свои всегда соглашаются (правило 3).
- Синие убивают только доминирующий вид (кроме себя, зелёных и голубых);
  белые убивают только «смешанных», своих не трогают; чёрные не убивают
  своих гибридов.
- Поле 888 слотов, старт — 5 чибиков каждого исходного вида.

## Как проверялось

Статистика получена утилитой `tools/balance.hs` (100–500 партий на разных
случайных зернах; проверяются условия завершения и инварианты
«свободно + население = 888», а также средний возраст смерти по видам).
Колебания численности — утилитой `tools/osc.hs` (число циклов бум-спад).
Запуск:

```sh
.build/balance rules/8885A.rule 100
.build/osc rules/triplet.rule 30 300
```

(сборка: см. `tools/balance.hs` в начале файла).
