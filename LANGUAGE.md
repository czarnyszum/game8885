# Язык правил (DSL) игры 8885

Документация по языку описания правил, на котором формализованы правила
игры из `OriginalRules.md`. Готовые наборы правил лежат в каталоге `rules/`
(`8885.rule` — полные правила игры, `example.rule` — небольшой пример).

Язык определяет: исходные виды и гибриды, параметры среды (размер поля,
стартовые числа, порог победы и т.п.), поведение видов (вероятность убийства,
выбор партнёра, цели убийства), симпатии (согласие на размножение), исходы
рождения и цвета для отображения.

---

## 1. Структура файла

Файл правил — набор **секций**, каждая из которых начинается с ключевого
слова и, как правило, содержит тело в фигурных скобках `{ ... }`. Секции
могут идти в любом порядке; единственная обязательная — `Базовые виды:`.
Каждая запись внутри секции заканчивается точкой с запятой `;`.

Комментарии: `/* ... */` (многострочные, вложенные) и `//` (до конца строки).

Пример структуры:

```
/* Файл с правилами */
Базовые виды: Красный, Синий, Белый, Чёрный, Жёлтый;

Синонимы: {
    Красный x Синий ~ Фиолетовый;
}

Параметры: {
    Поле: 888;
    Начало: 5;
}
```

## 2. Лексика

- **Идентификаторы** — названия видов и имена параметров. Могут содержать
  буквы (включая кириллицу), цифры, символы `_`, `'` и дефис `-`
  (например, `Тёмно-синий`, `Светло-жёлтый`).
- **Переменные** — идентификаторы, записанные **заглавными буквами**
  (`X`, `Y`, `S`, `T`). Переменная в правиле связывается с конкретным видом;
  одинаковые переменные в одном правиле должны означать один и тот же вид.
- **Специальные токены** — значения в тильдах `~...~`, см. раздел 4.
- `*` — «любой вид» (в позиции значения) или правило по умолчанию
  (в позиции имени правила).
- Зарезервированное слово: `кроме`.

## 3. Паттерны

Паттерн описывает вид (или пару видов для гибрида):

| Паттерн        | Значение                                            |
|----------------|-----------------------------------------------------|
| `*`            | любой вид                                           |
| `Красный`      | конкретный вид (по имени)                           |
| `X`            | переменная — любой вид, связывается с `X`           |
| `(Красный, X)` | гибрид, один из компонентов которого — `Красный`    |
| `(X, Y)`       | гибрид из видов, связанных с `X` и `Y`              |
| `~я~`          | свой собственный вид (вид действующего чибика)      |
| `~родители~`   | родительские цвета гибрида (для чистого вида — пусто) |
| `~чистый~`     | любой исходный (чистый) вид                         |
| `~гибрид~`     | любой гибридный вид                                 |
| `~доминирующий~` | вид с максимальной численностью (при равенстве — случайный из максимумов) |
| `~смешанные~`  | виды, чибики которых только что размножались с чужим цветом |

Паттерн `(A, B)` сопоставляется только с гибридами; одноэлементные паттерны
(`*`, имя, переменная, `~...~`) — с любым видом.

## 4. Секции

> Примеры в разделах — фрагменты: все упомянутые имена видов должны быть
> определены в секциях `Базовые виды:` и `Синонимы:` конкретного файла.
> В полном `rules/8885.rule` определены все 5 исходных и 10 гибридных видов,
> поэтому его секции самодостаточны.

### 4.1. `Базовые виды:`

Список исходных видов через запятую.

```
Базовые виды: Красный, Синий, Белый, Чёрный, Жёлтый;
```

### 4.2. `Синонимы:` — гибриды

Определение гибридного вида из двух исходных. Форма: `A x B ~ Имя;`.
Компоненты записываются в любом порядке (порядок нормализуется).

```
Синонимы: {
    Красный x Синий ~ Фиолетовый;
    Красный x Жёлтый ~ Оранжевый;
    Жёлтый x Белый ~ Светло-жёлтый;
}
```

Гибрид можно получить только из двух **разных исходных** видов; имена не
должны совпадать с уже занятыми.

### 4.3. `Параметры:` — переменные среды

Форма: `Имя: значение;` Значение — целое число или процент (с `%`).
Имя параметра может состоять из нескольких слов (например, `Максимум шагов`).

| Параметр          | Смысл                                                          | По умолчанию |
|-------------------|----------------------------------------------------------------|--------------|
| `Поле`            | количество слотов на поле                                      | 888          |
| `Начало`          | стартовая численность каждого исходного вида                   | 5            |
| `Победа`          | численность вида, при достижении которой игра завершается победой | 555       |
| `Изнасилование`   | вероятность того, что при отказе партнёра размножение всё равно произойдёт (иначе партнёр убивает инициатора) | 50% |
| `Максимум шагов`  | лимит числа шагов; 0 — без лимита                              | 0            |
| `Награда за убийство` | на сколько (в процентных пунктах) успешное убийство снижает вероятность смерти убийцы | 5% |
| `Штраф за размножение` | на сколько (в процентных пунктах) успешное размножение (с ≥1 потомком) повышает вероятность смерти родителя | 10% |

```
Параметры: {
    Поле: 888;
    Начало: 5;
    Победа: 555;
    Изнасилование: 50%;
    Максимум шагов: 0;
    Награда за убийство: 5%;
    Штраф за размножение: 10%;
}
```

Условия завершения игры привязаны к параметрам: поле полностью заполнено
(свободных слотов нет), вымирание (на поле менее 5 чибиков), победа вида
(численность вида ≥ `Победа`).

### 4.4. `Цвета:` — цвета для отображения

Форма: `Имя ~ #rrggbb;` Цвета задаются для исходных и гибридных видов и
используются интерфейсом для линий графика.

```
Цвета: {
    Красный ~ #d34747;
    Синий ~ #365d8a;
    Фиолетовый ~ #503c91;
}
```

### 4.5. `Действия:` — вероятность убийства

Форма: `Имя: N%;` (процент — вероятность выбрать убийство вместо
размножения), `Имя: ~смешанные~;` (особое поведение белых) или их сочетание
`Имя: N% ~смешанные~;` (убивать «смешанных» с вероятностью N%).
Синоним названия секции: `Склонность к убийству:`.
Имя `*` задаёт правило по умолчанию для всех видов без собственного правила
(по умолчанию, если правило не задано вовсе, — 0%, то есть вид никогда не
убивает).

```
Действия: {
    Красный: 0%;
    Синий: 15%;
    Жёлтый: 30%;
    Чёрный: 10%;
    Белый: 20% ~смешанные~;
    *: 0%;
}
```

- `N%` — вид с вероятностью `N%` выбирает убийство и с `100−N%` — размножение.
- `~смешанные~` — вид убивает, только если на поле есть чибики других видов,
  которые только что размножались с чужим цветом; иначе размножается
  (поведение белых).
- `N% ~смешанные~` — вид с вероятностью `N%` убивает «смешанных» (если они
  есть), в остальном размножается (используется в сбалансированном наборе
  правил `rules/8885A.rule`).

Если выбранное действие невозможно (нет жертвы, нет партнёра, поле заполнено),
выполняется другое действие; если невозможны оба — чибик пропускает ход.
Если убийство выбрано, но **не удалось** (см. `Успех убийства:`), ход
считается потраченным — размножение в этот ход не выполняется.

### 4.6. `Смертность:` — вероятность смерти при рождении

Форма: `Имя: N%;` — стартовая вероятность смерти чибика этого вида в конце
первого же хода его жизни. Имя `*` задаёт правило по умолчанию для видов без
собственной записи (по умолчанию — 1%).

```
Смертность: {
    Красный: 1%;
    Синий: 2%;
    Белый: 2%;
    *: 2%;
}
```

### 4.7. `Долголетие:` — шкала старения

Форма: `Имя: N;` — шкала долголетия `L` (целое число ходов). Каждый ход
вероятность смерти выжившего чибика растёт на `1/L` (до максимума 100%).
Ожидаемая продолжительность жизни свободного чибика примерно
`sqrt(pi*L/2)` ходов (при стартовой смертности `p0`), поэтому `L` легко
подобрать под желаемую продолжительность: `L ≈ 2*E^2/pi`. Имя `*` задаёт
правило по умолчанию (по умолчанию — 60).

```
Долголетие: {
    Красный: 150;
    Синий: 120;
    *: 150;
}
```

### 4.8. `Успех убийства:` — вероятность успеха убийства

Форма: `Имя: N%;` — вероятность того, что попытка убийства чибика этого вида
удастся (жертва выбрана по секции `Убийство:`, но может «уцелеть»). При
неудаче ход потрачен, жертва не умирает, награда не начисляется. Имя `*`
задаёт правило по умолчанию (по умолчанию — 100%).

```
Успех убийства: {
    Красный: 100%;
    Синий: 70%;
    Белый: 80%;
    *: 70%;
}
```

### 4.9. `Потомство:` — число потомков за размножение

Форма: `Имя: 0: N0% 1: N1% 2: N2% ...;` — распределение вероятностей числа
потомков за одно успешное размножение (включая ноль). Вероятности должны
суммироваться ровно в 100%. Дети создаются по одному, пока есть свободные
слоты (при нехватке слотов родится меньше, чем выпало). При нуле потомков
размножение «не состоялось»: потомок не рождается, родители не
блокируются, штраф за размножение не применяется. Имя `*` задаёт правило
по умолчанию (по умолчанию — `0: 0% 1: 100%`, то есть ровно один потомок).

```
Потомство: {
    Красный: 0: 20% 1: 60% 2: 20%;
    Синий: 0: 10% 1: 80% 2: 10%;
    *: 0: 20% 1: 60% 2: 20%;
}
```

### 4.10. `Партнёры:` — выбор партнёра при размножении

Форма: `Имя: [предпочтительные] -> [запасные] [условие];`

- `[предпочтительные]` — список токенов; партнёр выбирается **равномерно
  среди видов** из этого списка, у которых есть свободные (не заблокированные
  в этом ходу) чибики. Действующий чибик не может выбрать сам себя.
- Если предпочтительных нет — используются `[запасные]`.
- `[условие]` (необязательно) ограничивает **запасные**: если условие ложно,
  запасные недоступны.
- Имя `*` — правило по умолчанию для всех видов без собственного правила
  (по умолчанию — `[ * ] -> []`, то есть любой вид).

```
Партнёры: {
    Красный: [*] -> [];
    Синий: [Синий, Белый, Жёлтый] -> [*];
    Белый: [Белый] -> [~чистый~] [Белый < 5];
    Чёрный: [Чёрный, ~гибрид~] -> [];
    Жёлтый: [Жёлтый, Красный] -> [*];
    *: [~я~, ~родители~] -> [*];
}
```

Здесь: синие предпочитают своих, белых и жёлтых (а если их нет — любого);
белые размножаются только со своими, а если белых меньше 5 и свободных белых
партнёров нет — с другим чистым видом; чёрные — со своими и гибридами
(с другими чистыми по своей инициативе не размножаются); жёлтые — с жёлтыми
и красными поровну; гибриды (правило `*`) — с собой и родительскими цветами.

### 4.11. `Убийство:` — кого можно убивать

Форма: `Имя: токен, токен ... кроме Имя, Имя ...;`

- Токены задают множество возможных целей (вид считается целью, если
  соответствует хотя бы одному токену).
- `кроме` — исключения (конкретные виды).
- Жертва выбирается **среди особей** подходящих видов (вид взвешен по
  численности); чибик не может убить сам себя.
- Виды без правила (и правило `*`, если оно не задано) никогда не убивают.

```
Убийство: {
    Синий: ~доминирующий~ кроме Синий, Зелёный, Голубой;
    Белый: ~смешанные~ кроме Белый;
    Чёрный: * кроме Чёрный, Тёмно-синий, Коричневый, Бордовый, Серый;
    Жёлтый: * кроме Жёлтый, Белый;
}
```

Синие убивают только доминирующий вид (кроме себя, зелёных и голубых);
белые — только «смешанных» (кроме себя); чёрные — кого угодно, кроме себя и
своих гибридов; жёлтые — кого угодно, кроме себя и белых.

### 4.12. `Симпатии:` — согласие принимающей стороны

Форма: `Инициатор < Принимающий: вероятность% [условие] ...;`

Вероятность, с которой **принимающая** сторона соглашается на размножение
с инициатором. Одна запись может содержать несколько альтернатив
(паттерн + вероятность + условие): применяется первая подходящая.

Встроенные правила и умолчания:

- свой вид всегда согласен — **100%** (правило 3; проверяется первым);
- если ни одно правило не подошло — **50%** (нейтральное отношение).

Правила сортируются по конкретности паттерна инициатора (самые конкретные
первыми); применяется первое подходящее.

```
Симпатии: {
    * < Красный: 100%;
    ~гибрид~ < Белый: 0%;
    * < Белый: 80% [Белый < 5];
    * < Белый: 0%;
    ~гибрид~ < Чёрный: 100%;
    * < Чёрный: 50%;
    * < Жёлтый: 100%;
    Белый < Синий: 100%;
    Жёлтый < Синий: 100%;
    Красный < Синий: 50%;
    X < (X, *): 100%;
}
```

Разбор примера:

- красный как принимающая сторона соглашается всегда (`* < Красный: 100%`);
- белый не размножается с гибридами (`~гибрид~ < Белый: 0%`); с чистыми —
  80%, если белых меньше 5, иначе 0%;
- чёрный соглашается с гибридами всегда, с чистыми — 50/50;
- жёлтый как принимающая сторона соглашается всегда;
- синий соглашается с белыми и жёлтыми всегда, с красными — 50/50;
- гибрид соглашается со своим родительским цветом всегда
  (`X < (X, *)` — инициатор `X`, принимающий — гибрид, содержащий `X`).

Условия задаются в квадратных скобках: `[Вид < N]`, `[Вид <= N]`,
`[Вид > N]`, `[Вид >= N]` — сравнивается текущая численность вида.

При отказе партнёра срабатывает механика «изнасилования»: с вероятностью
`Изнасилование` (параметр, по умолчанию 50%) размножение всё равно
происходит, иначе партнёр убивает инициатора.

### 4.13. `Рождение:` — какой вид рождается

Форма: `Инициатор < Партнёр -> Результат: вероятность% ...;`

Перечисляются возможные исходы размножения пары (инициатор, партнёр) с
вероятностями. Правила упорядочиваются по конкретности (пары с конкретными
видами — раньше общих); применяется первое подходящее; среди его исходов
выбирается результат по указанным вероятностям.

Встроенные правила и умолчания:

- размножение чибиков одного вида даёт тот же вид — **100%**;
- если ни одно правило не подошло — **50/50** между родителями.

```
Рождение: {
    Красный < Синий -> Фиолетовый: 50% Красный: 25% Синий: 25%;
    Красный < Жёлтый -> Оранжевый: 50% Красный: 25% Жёлтый: 25%;
    Белый < Чёрный -> Серый: 50% Белый: 25% Чёрный: 25%;
    X < Y -> X: 50% Y: 50%;
}
```

Правило `X < Y -> X: 50% Y: 50%` — общее: потомок — цвет одного из родителей
(50/50). Конкретные правила (два исходных вида) перекрывают его: 50% гибрид,
по 25% каждый родитель. Для гибридных родителей работает только общее правило
(потомок — цвет одного из родителей), что соответствует правилу 4
OriginalRules.md.

Результат может быть и гибридом из компонентов: `(X, Y)` — гибрид видов,
связанных с `X` и `Y` (должен существовать среди определённых видов).

## 5. Как правила применяются (семантика шага)

1. На каждом шаге каждый живой чибик (в случайном порядке) выполняет одно
   действие: **размножение** или **убийство**.
2. Действие выбирается по секции `Действия:` (вероятность убийства;
   `~смешанные~` для белых).
3. **Размножение**: выбирается партнёр (секция `Партнёры:`), определяется
   согласие принимающей стороны (секция `Симпатии:`). При отказе — механика
   изнасилования. Число потомков определяется по секции `Потомство:`
   (секция `Рождение:` задаёт, какого вида будет каждый потомок); для
   рождения нужен свободный слот — дети создаются по одному, пока слоты
   есть. Если потомков больше нуля: родители и все дети до конца хода
   **заблокированы** для выбора партнёром (правило 2), но их можно убить;
   если родители разных видов, они помечаются как «смешанные» — цель белых;
   вероятность смерти инициатора растёт на `Штраф за размножение`.
4. **Убийство**: выбирается жертва (секция `Убийство:`); попытка убийства
   удаётся с вероятностью из секции `Успех убийства:` (при неудаче ход
   потрачен, жертва жива). При успехе жертва погибает, освобождая слот,
   а вероятность смерти убийцы падает на `Награда за убийство`.
5. **Конец хода (конечная жизнь)**: каждый чибик умирает со своей текущей
   вероятностью смерти (возраст смерти записывается в гистограмму вида);
   выжившие стареют на 1 ход, и их вероятность смерти растёт на `1/L`
   (`L` — `Долголетие:` вида). Освободившиеся слоты снова свободны.
6. Игра завершается, когда: поле заполнено полностью; на поле осталось
   менее 5 чибиков; численность какого-либо вида достигла `Победа`
   (проверяется и после конца хода — вымирание от старости).

## 6. Полный пример — `rules/8885.rule`

```text
/* Правила игры 8885 (OriginalRules.md) */

Базовые виды: Красный, Синий, Белый, Чёрный, Жёлтый;

Синонимы: {
    Красный x Синий ~ Фиолетовый;
    Красный x Жёлтый ~ Оранжевый;
    Красный x Белый ~ Розовый;
    Красный x Чёрный ~ Бордовый;
    Синий x Жёлтый ~ Зелёный;
    Синий x Белый ~ Голубой;
    Синий x Чёрный ~ Тёмно-синий;
    Жёлтый x Белый ~ Светло-жёлтый;
    Жёлтый x Чёрный ~ Коричневый;
    Белый x Чёрный ~ Серый;
}

Параметры: {
    Поле: 888;
    Начало: 5;
    Победа: 555;
    Изнасилование: 50%;
    Максимум шагов: 0;
    Награда за убийство: 5%;
    Штраф за размножение: 10%;
}

Цвета: {
    Красный ~ #d34747;
    Синий ~ #365d8a;
    Белый ~ #f3f3f3;
    Чёрный ~ #8f8f8f;
    Жёлтый ~ #d9c03c;
    Фиолетовый ~ #503c91;
    Оранжевый ~ #e8913b;
    Розовый ~ #f57ba3;
    Бордовый ~ #8f0d0d;
    Зелёный ~ #7ca924;
    Голубой ~ #76b8b8;
    Тёмно-синий ~ #24527a;
    Светло-жёлтый ~ #e6df8b;
    Коричневый ~ #8b5a2b;
    Серый ~ #c0c0c0;
}

Действия: {
    Красный: 0%;
    Синий: 15%;
    Белый: ~смешанные~;
    Чёрный: 10%;
    Жёлтый: 30%;
    *: 0%;
}

Смертность: {
    Красный: 1%;
    Синий: 2%;
    Белый: 2%;
    Чёрный: 2%;
    Жёлтый: 2%;
    *: 2%;
}

Долголетие: {
    Красный: 150;
    Синий: 120;
    Белый: 180;
    Чёрный: 120;
    Жёлтый: 130;
    *: 150;
}

Успех убийства: {
    Красный: 100%;
    Синий: 70%;
    Белый: 80%;
    Чёрный: 60%;
    Жёлтый: 70%;
    *: 70%;
}

Потомство: {
    Красный: 0: 20% 1: 60% 2: 20%;
    Синий: 0: 20% 1: 60% 2: 20%;
    Белый: 0: 20% 1: 60% 2: 20%;
    Чёрный: 0: 20% 1: 60% 2: 20%;
    Жёлтый: 0: 20% 1: 60% 2: 20%;
    *: 0: 20% 1: 60% 2: 20%;
}

Партнёры: {
    Красный: [*] -> [];
    Синий: [Синий, Белый, Жёлтый] -> [*];
    Белый: [Белый] -> [~чистый~] [Белый < 5];
    Чёрный: [Чёрный, ~гибрид~] -> [];
    Жёлтый: [Жёлтый, Красный] -> [*];
    *: [~я~, ~родители~] -> [*];
}

Убийство: {
    Синий: ~доминирующий~ кроме Синий, Зелёный, Голубой;
    Белый: ~смешанные~ кроме Белый;
    Чёрный: * кроме Чёрный, Тёмно-синий, Коричневый, Бордовый, Серый;
    Жёлтый: * кроме Жёлтый, Белый;
}

Симпатии: {
    * < Красный: 100%;
    ~гибрид~ < Белый: 0%;
    * < Белый: 80% [Белый < 5];
    * < Белый: 0%;
    ~гибрид~ < Чёрный: 100%;
    * < Чёрный: 50%;
    * < Жёлтый: 100%;
    Белый < Синий: 100%;
    Жёлтый < Синий: 100%;
    Красный < Синий: 50%;
    X < (X, *): 100%;
}

Рождение: {
    Красный < Синий -> Фиолетовый: 50% Красный: 25% Синий: 25%;
    Красный < Жёлтый -> Оранжевый: 50% Красный: 25% Жёлтый: 25%;
    Красный < Белый -> Розовый: 50% Красный: 25% Белый: 25%;
    Красный < Чёрный -> Бордовый: 50% Красный: 25% Чёрный: 25%;
    Синий < Жёлтый -> Зелёный: 50% Синий: 25% Жёлтый: 25%;
    Синий < Белый -> Голубой: 50% Синий: 25% Белый: 25%;
    Синий < Чёрный -> Тёмно-синий: 50% Синий: 25% Чёрный: 25%;
    Жёлтый < Белый -> Светло-жёлтый: 50% Жёлтый: 25% Белый: 25%;
    Жёлтый < Чёрный -> Коричневый: 50% Жёлтый: 25% Чёрный: 25%;
    Белый < Чёрный -> Серый: 50% Белый: 25% Чёрный: 25%;
    X < Y -> X: 50% Y: 50%;
}
```

## 7. Минимальный пример — `rules/example.rule`

```text
Базовые виды: Красный, Синий;

Синонимы: {
    Красный x Синий ~ Фиолетовый;
}

Параметры: {
    Поле: 100;
    Начало: 5;
    Победа: 60;
    Изнасилование: 50%;
    Максимум шагов: 1000;
    Награда за убийство: 10%;
    Штраф за размножение: 20%;
}

Цвета: {
    Красный ~ #d34747;
    Синий ~ #365d8a;
    Фиолетовый ~ #503c91;
}

Действия: {
    Красный: 0%;
    Синий: 30%;
    Фиолетовый: 10%;
}

Смертность: {
    Красный: 5%;
    Синий: 5%;
    Фиолетовый: 5%;
    *: 5%;
}

Долголетие: {
    Красный: 60;
    Синий: 60;
    Фиолетовый: 60;
    *: 60;
}

Успех убийства: {
    Красный: 100%;
    Синий: 60%;
    Фиолетовый: 60%;
    *: 60%;
}

Потомство: {
    Красный: 0: 40% 1: 60%;
    Синий: 0: 40% 1: 60%;
    Фиолетовый: 0: 40% 1: 60%;
    *: 0: 40% 1: 60%;
}

Партнёры: {
    Красный: [*] -> [];
    Синий: [Синий] -> [*];
    *: [~я~] -> [*];
}

Убийство: {
    Синий: * кроме Синий, Фиолетовый;
    Фиолетовый: * кроме Фиолетовый, Красный, Синий;
}

Симпатии: {
    * < Красный: 100%;
    * < Синий: 50%;
    X < (X, *): 100%;
}

Рождение: {
    Красный < Синий -> Фиолетовый: 50% Красный: 25% Синий: 25%;
    X < Y -> X: 50% Y: 50%;
}
```

## 8. Примечания

- **Правило по умолчанию `*`** в секциях `Действия:`, `Партнёры:`,
  `Убийство:`, `Смертность:`, `Долголетие:`, `Успех убийства:`,
  `Потомство:` задаёт поведение для всех видов, у которых нет собственной
  записи. Без него: вероятность убийства 0%, партнёр — любой вид, убийство
  невозможно, смертность 1%, долголетие 60, успех убийства 100%, потомство —
  ровно один ребёнок.
- **Проценты** в `Симпатиях:`, `Рождении:`, `Смертности:` и `Успехе
  убийства:` — абсолютные вероятности (не веса); в `Действиях:` —
  вероятность убийства; в `Потомстве:` — распределение числа детей
  (должно суммироваться в 100%); `Награда за убийство` и `Штраф за
  размножение` — процентные пункты прироста/снижения вероятности смерти.
- **Специфичность**: в `Рождении:` пары с конкретными видами применяются
  раньше общих (с переменными); в `Симпатиях:` раньше применяются правила
  с более конкретным паттерном инициатора.
- **Переменные** должны связываться непротиворечиво: `X < X` — только
  размножение чибиков одного и того же вида; `X < (X, *)` — партнёр-гибрид,
  содержащий вид инициатора.
- Валидность файла проверяется при запуске; ошибки (неизвестные имена,
  некорректные проценты, сумма `Потомства` ≠ 100% и т.п.) выводятся
  в статусную строку интерфейса и в сообщения WebSocket
  (`{"type":"error",...}`).
