# План и архитектура (game8885)

## Цель

Zero-player симуляция "Игра 8885" (см. `OriginalRules.md`): поле 888 слотов,
5 исходных видов, размножение/убийство, гибриды, поведение видов, условия
завершения. Правила формализованы в DSL (`rules/*.rule`), симуляция на Haskell,
UI на WebSocket + ApexCharts. Дополнительно (UPDATE0.md): **конечная жизнь**
каждого чибика — возраст и вероятность смерти, закон старения по видам,
недетерминированные убийство и размножение, награды/штрафы, гистограммы
продолжительности жизни в UI (математика и симуляции — `SIMS.md`).

## Конечная жизнь (решения)

1. **Индивидуальная модель**: население хранится по особям
   (`_population :: Map (Species b) [Life]`); каждый чибик несёт возраст
   и текущую вероятность смерти.
2. **Закон старения (4.1)**: вероятность смерти растёт линейно на `1/L`
   каждый ход: `p(t) = min(1, p0 + t/L)`; `p0` (Смертность) и `L`
   (Долголетие) — по видам в rules-файле. Ожидаемая продолжительность
   жизни свободного чибика ≈ `sqrt(pi*L/2)`; анализ в `SIMS.md`.
3. **Смерть в конце хода (4.0)**: каждый чибик умирает со своей текущей
   вероятностью в конце хода; выжившие стареют; возраст смерти пишется
   в гистограмму вида (обновляется каждый ход, 4.6).
4. **Успех убийства (4.2)**: вероятность успеха попытки убийства — по видам
   (`Успех убийства:`); при неудаче ход потрачен.
5. **Награда за убийство (4.3)**: успешное убийство снижает вероятность
   смерти убийцы на глобальный параметр `Награда за убийство`.
6. **Потомство (4.4)**: распределение числа детей по видам (`Потомство:`);
   при нуле детей штраф не применяется; дети создаются, пока есть слоты.
7. **Штраф за размножение (4.5)**: успешное размножение (≥1 потомок)
   повышает вероятность смерти инициатора на глобальный параметр
   `Штраф за размножение`.
8. **Гистограммы (4.6)**: `space.histogram` (вид → возраст → число умерших);
   шлются в `init`/`state` WebSocket-сообщениях как `lifespans`;
   frontend рисует столбчатую гистограмму по видам.

## Интерпретация правил (решения)

1. **Шаг** — каждый живой чибик выполняет ровно одно действие (размножиться
   или убить) в случайном порядке. Подтверждено пользователем.
2. **Блокировка (правило 2)** — после размножения оба родителя и ребёнок
   помечаются заблокированными до конца хода: их нельзя выбрать партнёром,
   но можно убить; сами они действовать могут.
3. **«Заняты» у белых** — «свои уже все заняты» = все белые заблокированы
   (только что размножались). Белый инициирует с другим чистым цветом только
   если белых < 5 и нет свободного белого партнёра.
4. **Жёлтые** — выбирают красного и жёлтого с равной вероятностью; если их
   нет — любого другого.
5. **Смешанное потомство** — исходный x исходный: 50% гибрид, 25/25 родители;
   гибрид x гибрид и исходный x гибрид: 50/50 цвет одного из родителей.
6. **Белые убивают** — только не-белых чибиков, которые только что
   размножились с чужим цветом («своих не убивают»).
7. **Синие убивают** — только доминирующий вид (при равенстве — случайный из
   максимумов), исключая себя, зелёных и голубых.
8. **«Своих гибридов» у чёрных** — гибриды, содержащие чёрный (тёмно-синий,
   коричневый, бордовый, серый).
9. **Партнёр выбирается по видам** (вид-равномерно внутри набора), **жертва —
   по особям** (взвешено по численности). Правило 4 для жёлтых требует
   видового уровня, поэтому партнёр — вид-равномерно.
10. **Согласие** (симпатия): свои всегда 100%; иначе первое подходящее
    правило DSL; иначе 50/50 по умолчанию. Отказ → 50/50: изнасилование
    (рождение) или партнёр убивает инициатора.

## DSL (правила)

Подробное описание языка правил с примерами — в `LANGUAGE.md`.

Секции: `Базовые виды`, `Синонимы` (гибриды), `Параметры` (Поле, Начало,
Победа, Изнасилование, Максимум шагов, Награда за убийство, Штраф за
размножение), `Цвета`, `Действия` (вероятность убийства; `~смешанные~` для
белых), `Смертность`, `Долголетие`, `Успех убийства`, `Потомство`,
`Партнёры` (предпочтения инициатора), `Убийство` (цели и исключения),
`Симпатии` (согласие принимающей стороны), `Рождение` (исходы).
Специальные токены: `*`, `~я~`, `~родители~`, `~чистый~`, `~гибрид~`,
`~доминирующий~`, `~смешанные~`. Условия: `[Вид < N]` и т.п.

Полные правила игры — `rules/8885.rule`; сбалансированный вариант с
обоснованием изменений — `rules/8885A.rule` и `ChangeLog.md`;
минимальная модель для тестирования конечной жизни — `rules/triplet.rule`;
пример — `rules/example.rule`. Статистический инструмент балансировки —
`tools/balance.hs` (доли видов в финале + средний возраст смерти).

## Модули

- `Species.hs` — типы: вид (чистый/гибрид), действие, жизнь чибика (возраст,
  вероятность смерти), поле (особи, свободные слоты, заблокированные,
  «смешанные», гистограмма), параметры среды.
- `Pattern.hs` — паттерны и контекстное сопоставление, типы правил, `Tables`
  (включая карты Смертность/Долголетие/Успех убийства/Потомство).
- `Sim.hs` — симуляция: выбор действия/партнёра/жертвы, согласие, рождение,
  изнасилование, успех убийства, потомство, старение и смерть в конце хода,
  гистограммы, условия завершения, `stepTurn`.
- `Decl.hs` — декларации DSL и компилятор (правила → `Tables`).
- `Parser.hs` — парсер DSL.
- `Cmd.hs` — протокол WebSocket (`init/start/pause/restart/step/select/list`,
  сообщения `hello/init/state/error`; `init`/`state` несут `lifespans`).
- `Ctx.hs` — состояние игры, тикер автошагов, обработка команд, рассылка.
- `Server.hs`, `MainPage.hs` — HTTP-сервер Snap и страница (управление +
  статус + график популяций + гистограммы жизни).
- `Test.hs`, `TestMain.hs` — тесты (вероятности рождения 50/25/25, 50/50,
  симпатии, инварианты, старение, успех убийства, потомство, полные партии,
  triplet).
- `public/js/main.js` — UI (график популяций + гистограмма жизни).

## Исправленные ошибки исходного кода

- `rapeAction`: использовала `killRatio` вместо 50/50; при отказе «умирал» не
  тот чибик и свободное место уменьшалось вместо увеличения.
- `killAction`: вероятность убийства сэмплировалась дважды; население
  менялось неверно.
- Отсутствовали: проверка свободных слотов, блокировка (правило 2), трекинг
  «смешанных», условия завершения, интерпретатор правил.
- Парсер молча обрезал секции при ошибке (добавлен `eof`, `;` после строк);
  имена с дефисом (`Тёмно-синий`); параметр `Максимум шагов`; синонимы не
  нормализовались (`mix`).
- Проценты в `Симпатиях` трактовались как веса (50% → 100%) — теперь это
  абсолютные вероятности.

## Сборка/запуск

Корневая ФС может быть read-only (stack не пишет в ~/.stack) — сборка
напрямую GHC из снапшотов: `./build.sh`, `./test.sh`, `./run.sh`
(сервер на http://127.0.0.1:8000). В writable-окружении: `stack build`.

## Статус

- [x] Правила проанализированы, вопросы уточнены.
- [x] DSL расширен (параметры среды, действия, партнёры, убийство, симпатии
      с условиями, спец-токены) — все правила OriginalRules.md выразимы.
- [x] Конечная жизнь: индивидуальная модель, закон старения, успех убийства,
      потомство, награда/штраф, гистограммы — ядро, DSL, rules-файлы,
      тесты, `SIMS.md`.
- [x] Ядро симуляции переписано; вероятности исправлены; блокировка,
      «смешанные», условия завершения (включая вымирание от старости).
- [x] rules/8885.rule — полные правила; тесты проходят.
- [x] Сервер Snap + WebSocket: команды, поток состояния (включая lifespans),
      рассылка, тикер.
- [x] MainPage: управление/статус/график популяций + гистограммы жизни;
      стили дополнены минимально.
- [x] Frontend UI (main.js) — график популяций + гистограмма продолжительности
      жизни, обновление после каждого шага.
- [x] Подбор параметров для интересной динамики (triplet.rule и 8885A).
