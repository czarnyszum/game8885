# SIMS.md — конечная жизнь: математика и симуляции

Документ обосновывает выбор закона старения (п. 4.1 UPDATE0.md), законы
награды за убийство (4.3) и штрафа за размножение (4.5) и параметры,
подобранные для интересной динамики (4.9). Все симуляции запускаются
инструментами `tools/lifespan.hs`, `tools/osc.hs` и `tools/balance.hs`
(сборка напрямую GHC, как в `build.sh`).

## 1. Модель (п. 4.0)

Каждый чибик несёт два индивидуальных атрибута:

* `age` — число ходов, которое он прожил;
* `p` — вероятность умереть в конце текущего хода.

В **конце каждого хода** (после того как все чибики выполнили действие)
происходит «проверка смерти»: чибик умирает с вероятностью `p` (возраст
смерти записывается в гистограмму его вида, слот освобождается), выживший
стареет на 1 ход, а его `p` растёт по закону старения. В течение хода
`p` меняется событиями: успешное убийство снижает его на `Награда за
убийство`, успешное размножение (≥ 1 потомок) повышает на `Штраф за
размножение`.

## 2. Выбор закона старения (п. 4.1)

Требование: вероятность смерти растёт каждый ход, а **ожидаемая
продолжительность жизни свободного чибика** (не убивающего и не
размножающегося) должна управляться параметрами, задаваемыми в `*.rules`
по видам.

Рассмотрены кандидаты:

| Закон | `p(t)` | Управляемость E[T] | Итог |
|---|---|---|---|
| Постоянный риск | `p0` | E = 1/p0, но p не растёт | ✗ (нарушает требование) |
| Линейный рост | `min(1, p0 + t/L)` | E ≈ √(πL/2), формула простая | ✓ |
| Геометрический рост (Гомперц) | `min(1, p0·b^t)` | аналитически громоздко | ~ |
| Логистический | `p_max/(1+e^{−k(t−t0)})` | 3 параметра, избыточно | ~ |

**Выбран линейный закон**: `p(t) = min(1, p0 + t/L)`, где

* `p0` — **Смертность** (вероятность смерти в конце первого хода жизни),
* `L` — **Долголетие** (шкала: каждый ход `p` растёт на `1/L`).

`p0` и `L` задаются по видам в секциях `Смертность:` и `Долголетие:`.

### 2.1. Выживаемость и ожидаемая продолжительность жизни

`S(t)` — вероятность дожить до возраста `t` (пережить проверки 0..t−1):

```
S(0) = 1
S(t) = ∏_{k=0}^{t−1} (1 − p0 − k/L),   p(t) = min(1, p0 + t/L)
```

Возраст смерти `T`: `P(T = t) = S(t)·p(t)`; ожидание
`E[T] = Σ_{t≥0} t·S(t)·p(t)` (ряд конечен, т.к. при `t ≥ L(1−p0)`
вероятность достигает 1).

**Приближение** (непрерывная релаксация, `S(t) ≈ exp(−p0·t − t²/2L)`):

```
E[T] ≈ ∫₀^∞ exp(−p0·t − t²/(2L)) dt
```

При `p0 = 0`: `E[T] ≈ √(πL/2)`. Формула для подбора шкалы под желаемую
продолжительность жизни: **`L ≈ 2·E²/π`**.

Точные значения (сумма ряда) для типичных параметров:

| p0 | L | E[T] (точно) | √(πL/2) |
|----|----|----|----|
| 0% | 60 | 9.39 | 9.71 |
| 2% | 60 | 8.21 | 9.71 |
| 2% | 120 | 11.18 | 13.73 |
| 2% | 150 | 12.30 | 15.35 |
| 2% | 200 | 13.86 | 17.72 |
| 5% | 60 | 6.79 | 9.71 |
| 1% | 150 | 13.56 | 15.35 |

Приближение `√(πL/2)` хорошо работает при малых `p0`; при `p0 ≥ 2–5%`
точная сумма на 15–30% меньше.

### 2.2. Валидация движка против теории

Инструмент `tools/lifespan.hs` гоняет только фазу старения/смерти
(без действий — «свободные» чибики) и сообщает средний возраст смерти:

| Параметры | Теория E[T] | Движок (средний возраст) |
|---|---|---|
| p0 = 2%, L = 200 | 13.86 | 13–14 |
| p0 = 5%, L = 60 | 6.79 | 7 |

Движок воспроизводит теорию (расхождение в пределах шума).

## 3. Награда за убийство и штраф за размножение (п. 4.3, 4.5)

Законы **глобальные** (одинаковы для всех видов), параметры — переменные
среды (`Награда за убийство`, `Штраф за размножение`), аддитивные в
процентных пунктах, как и сам закон старения:

```
убийство:    p := max(0, p − R)      (R — Награда за убийство)
размножение: p := min(1, p + C)      (C — Штраф за размножение)
```

Выбор аддитивных законов мотивирован аддитивностью закона старения
(`p` растёт на `1/L` в ход): все три механизма складываются в одной шкале,
и их относительная сила видна напрямую.

**Баланс сил.** В стационарном состоянии средний прирост `p` за ход от
старения (`1/L`) должен компенсироваться наградами убийств и
«накапливаться» штрафами размножения. Если чибик в среднем убивает `k`
раз в ход и размножается `r` раз в ход, равновесие:

```
1/L + r·C ≈ k·R
```

При `R = 10%`, `C = 20%`, `L = 60` (triplet): старение даёт +1.7%/ход;
одно размножение «стоит» 20% — чибик, размножившийся 3–4 раза, близок
к гарантированной смерти. Это механизм **репродуктивного истощения**:
бум размножения создаёт когорту родителей с высокой `p`, которая затем
вымирает скопом — популяция падает (спад), после чего молодые выжившие
размножаются снова (бум). Период цикла ~ время накопления `p` до ~1:
порядка `1/C` успешных размножений.

**Решения по применению** (зафиксированы для воспроизводимости):

* проверка смерти — в конце хода, с текущим `p` (после всех событий хода);
* награда применяется к убийце, штраф — к инициатору размножения
  (актору), сразу после события (влияет на ближайшую проверку смерти);
* промах при убийстве (неудачный бросок `Успех убийства:`) ход тратит:
  размножение в этот ход не выполняется;
* размножение с нулём потомков не считается успешным: штраф, блокировка
  и пометка «смешанных» не применяются.

## 4. Подбор параметров (п. 4.9) — минимальная модель triplet.rule

Стартовая точка — `rules/triplet.rule` (2 базовых вида + 1 гибрид).
Подбор велся инструментом `tools/osc.hs` (счётчик циклов бум-спад: спад
суммарной популяции на ≥30% от локального максимума с последующим
восстановлением на ≥130%) и `tools/balance.hs`.

Наблюдения при поиске:

* при «дешёвом» размножении (штраф мал, потомков много) популяция
  экспоненциально заполняет поле — партии из ~10 ходов, колебаний нет;
* при слишком дорогом — популяция не выживает (вымирание);
* колебания появляются узкой полосой около уровня воспроизводства:
  среднее число потомков ~0.55–0.6 при штрафе 18–22% и жизни 6–8 ходов.

Итоговые параметры triplet.rule:

| Параметр | Значение | Эффект |
|---|---|---|
| Смертность | 5% | стартовая смертность |
| Долголетие | 60 | E ≈ 6.8 хода (свободный чибик) |
| Потомство | 0: 45% 1: 55% | среднее 0.55, чуть ниже воспроизводства |
| Штраф за размножение | 20% | репродуктивное истощение |
| Награда за убийство | 10% | выгодно убивать доминанта |
| Успех убийства | 50% | убийство недетерминировано |
| Действия (убийство) | 30% | ~треть ходов — убийство |

Результат (30 партий, до 300 ходов):

```
исход: Поле полностью заполнено: 25; Вымирание: 2; не завершена (долгая): 3
длина партии: мин=30, медиана≈190, макс>300
партий с ≥2 циклами роста/спада: ~10–15 из 30 (в зависимости от семян)
```

Типичная партия: несколько бум-спад циклов суммарной популяции, состав
видов меняется (доминанта «подрезают» красные и синие, убивающие
доминирующий вид).

## 5. 8885A.rule — сбалансированный вариант

Те же идеи перенесены на полный набор из 15 видов (5 исходных + 10
гибридов): `Смертность 3–5%`, `Долголетие 60–80`, `Потомство 0:40/1:60`
(среднее 0.6), `Штраф 18%`, `Награда 10%`, `Успех убийства 50–70%`.
Результат (100 партий, `tools/balance.hs`):

```
Победа вида: Красный 21%, Жёлтый 14%, Бордовый 10%, Синий 10%,
             Розовый 8%, Серый 6%, Фиолетовый 3%, Чёрный 2%,
             Оранжевый/Голубой ~1%; заполнение поля 6%; вымирание 18%.
средняя длина партии ≈ 49 ходов (было 10);
доли в финале: Жёлтый 26%, Красный 14%, Оранжевый 9%, Синий 9%, ...
```

Победители разнообразны (9 разных видов), ни один вид не доминирует
(максимум — 26% поля у жёлтых). Вымирание в ~18% партий — следствие
глубоких спадов (приемлемая цена за длинные «качельные» партии).

## 6. Воспроизводимость

```sh
# теория (Python, раздел 2.1)
python3 - <<'EOF'
def E(p0, L):
    S=1.; e=0.; t=0
    while True:
        p=min(1., p0+t/L); e+=t*S*p; S*=1-p
        if S<=1e-15: break
        t+=1
    return e
print(E(0.02,200), E(0.05,60))
EOF

# валидация движка (раздел 2.2)
.build/lifespan rules/triplet.rule 400 40

# колебания (раздел 4)
.build/osc rules/triplet.rule 30 300

# баланс (раздел 5)
.build/balance rules/8885A.rule 100
```

Сборка инструментов: `"$GHC" -O1 -package-db "$PKGDB" -isrc tools/<имя>.hs -o .build/<имя>`
(GHC и PKGDB — как в `build.sh`).
